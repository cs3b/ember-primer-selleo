{{items-form onSubmit=(action 'add')}} 

{{items-filter onChange=(action (mut query))}}

<ul class="list measure center">
  {{#each itemsFiltered as |item|}}
    {{items-element thing=item onClick=(action 'archive' item)}}
  {{else}}
    Sorry, nothing here.
  {{/each}}
</ul>

{{outlet}}